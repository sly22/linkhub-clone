{
  "name": "link-checker",
  "version": "1.0.0",
  "description": "링크 자동 확인 앱 - links.json에 URL을 넣으면 자동으로 페이지 로딩 확인",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "postinstall": "npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "express": "^4.18.2",
    "playwright": "^1.41.0"
  }
}
